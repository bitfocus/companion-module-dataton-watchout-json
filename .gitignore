node_modules/
package-lock.json
/pkg
/pkg.tgz
DEBUG-*
.DS_Store
# ignore caches, but keep the release binary and plugins
.yarn/cache/
.yarn/build-state.yml
.yarn/install-state.gz

# EXCEPT these
!.yarn/releases/
!.yarn/plugins/
*.tgz
